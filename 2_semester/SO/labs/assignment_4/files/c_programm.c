#include <stdio.h>

int fact(int n)
{
    int i,f=1;
    for(i=1;i<=n;i++)
    {
        f=f*i;
    }
    return f;
}

void main()
{
	int no,factorial;
  	factorial=fact(5);
    int e = fact(5);
    printf("Factorial  %d\n",no,fact(n));
    fact1(n);
}